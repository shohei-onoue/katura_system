# katura_system 開発指示書（Claude用）

このファイルは本プロジェクトでClaude Codeが従うべき運用ルールを定義する。

## 1. 回答フォーマット（/diff-only）

- コード変更の回答は**差分（diff）またはコード変更箇所のみ**とする。
- 説明文・要約・前置き・後書きは付けない。
- 回答は端的に限りなく短くする。
- 回答は３行以内とし、絵文字は禁止とする。
- ユーザーはエンジニア初心者のため専門用語には簡単な解説を付け加える。
- 小学5年生でも分かるよう噛み砕いて回答・解説する
- ただし、以下は例外として許可する。
  - ユーザーが明示的に説明を求めた場合。
  - 承認が必要な操作（2章・3章）の確認事項を提示する場合。

## 2. UI/UX/デザイン変更の禁止

- ユーザーから明示的な指示がない限り、UI/UX/デザインの変更は一切行わない。
- ロジック修正・バグ修正の過程で見た目に影響する変更が必要になった場合は、実装前に必ずユーザーへ確認する。

## 3. 新規スキル・拡張機能の導入

- 開発に新しいスキル、パッケージ、拡張機能、ツールのインストールが必要な場合、**ユーザーの承認を得てから実行する**。
- 無断でのインストール・設定変更は行わない。
- 承認を求める際は、必要な理由と導入対象を簡潔に提示する。

## 4. デバッグ実行（/debag）

- 実機・エミュレータでのビルド・起動・再起動は、ユーザーが`/5000`で明示的に指示した場合のみ行う。指示なく自発的に実行しない。
- 回答はコードの差分のみを実行し、解説は極限まで短く、もしくは無しとする。

## 5. 指示内容の振り分け

- ユーザーの指示は.claude/agents/pm.mdが指示内容を確認し、サブエージェントへ作業を振り分けること。
- pm.mdは以下の条件から作業内容を振り分けること。
  - バックエンド作業が必要と判断したら.claude/agents/backend-engineer.mdへ指示を出すこと。
  - デザイン作業が必要と判断したら.claude/agents/designer.mdへ指示を出すこと。
  - ビルド作業が必要と判断したら.claude/agents/devops.mdへ指示を出すこと。
  - フロントエンド作業が必要と判断したら.claude/agents/frontend-engineer.mdへ指示を出すこと。
  - 作業が必要と判断したら.claude/agents/frontend-engineer.mdへ指示を出すこと。
- 変更された内容は.claude/agents/qa-tester.mdが品質確認をして完了とします。
- 変更された内容に問題があれば変種した.mdに修正依頼をし再チェックを実施すること。
